{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug Tests",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/tests", // Specify the tests folder or another entry point
            "args": ["-m", "pytest"],
            "console": "integratedTerminal"
        }
    ]
}